# HOST render environment only (GitHub Actions / local `quarto render`).
# All course logic comes from libdpy — the website defines none of it.
# libdpy itself is installed by dev/tools/sync_libdpy.sh from sibling code_base_dev/libdpy
# (editable). Student pip installs still pin pub_lib — see requirements-pub-lib-pin.txt.
# The BROWSER (WASM) path uses a hosted wheel via micropip/piplite — never git+ (v4 §1.1).
jupyter
numpy
scipy
pandas
matplotlib
pillow
plotly==6.8.0
narwhals==2.22.1
packaging==26.2
ipywidgets
anywidget
marimo==0.23.9
PyYAML>=6.0,<7
